para correr, fazer o seguinte:
correr split_data.py  --  cria folder com train e test datasets

depois cada ficheiro:
  recebe como input o train e test anterior (no primeiro, sao os raw)
  manda como output os melhores train e test criados

ordem para correr os sripts:
split_data -> missing_values -> encoding -> outliers -> scaling -> balancing -> features
